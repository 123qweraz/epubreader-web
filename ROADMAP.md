# 功能路线图（打字系列）

> 状态标记: [ ] 待做 / [~] 进行中 / [x] 完成。每阶段一个版本号递增(sw.js VERSION) + git commit。

## 阶段一：日语罗马音注音 [x]（v36/v37）

扩展注音系统支持日语罗马音，为打字模式铺路。

**v37 追加**: 纯拼音替换正文（annotate="pinyinOnly"，.pyRep span 音译替换+原生ruby一并替换）+ 大文件懒注音修复（任一目标块超PY_CAP按子元素递归拆分，整书模式大章节不再整章漏注）。

扩展注音系统支持日语罗马音，为打字模式铺路。

- [x] 方案定稿（见下设计要点）
- [x] `pinyin.js`: 假名正则 + wapuro 式罗马音转换表（拗音最长匹配/っ促音双写/ー长元音重复前元音/ん=n）+ `pyAnnotateNode` 按模式分支 + 书内原生 ruby rt(假名)→罗马音转换 pass
- [x] `reader.js`: `state.annotate`("off"/"pinyin"/"romaji")，旧 showPinyin=1 迁移为 "pinyin"；BACKUP_PREF_KEYS 增加 annotate；buildChapterDoc/runAfterLoad/syncAllPrefsUI 接线
- [x] `index.html`: 高级页拼音开关 → 三态分段控件（关/拼音/罗马音）
- [x] `reader.css` + `i18n.js`(zh/en): 分段控件样式与文案
- [x] `sw.js` → v36
- [x] `tests/smoke.mjs`: lang="ja" 测试书断言 ひらがな→hiragana / カタカナ→katakana；拼音回归测试适配新控件

**设计要点**:
- 单个汉字无法区分中日（同 Unicode 码位），采用三级语境判定：章节 lang 属性先验 → 段落含假名按日语 → 纯汉字段按先验
- 注音分流天然规避歧义：拼音模式只注汉字、罗马音模式只注假名（wapuro 式即打字输入习惯：おう→ou、っき→kki）
- 日语汉字读音无字典：裸字不注；书自带 ruby 的 rt 若为假名则转罗马音显示（《吾輩は猫である》全覆盖）
- 中日混排书各注各的，零依赖新增

## 阶段二：打字模式——照书打字驱动阅读 [x]（v38~v49）

用户只按 a-z：英文打字母（忽略空格与非字母）、中文打全拼无声调、日文打罗马音（复用阶段一转换器）。

**实现要点**（typing.js, 加载序 pinyin→typing→reader）:
- [x] Token 管线: 按块惰性包裹(当前段落才展开span), 英文词=连续字母run、汉字=单字无声调拼音、假名=拗音合并token; ruby取rt读音(假名→罗马音); 日语语境汉字自动跳过
- [x] 高亮: 当前token灰底待打/已打前缀亮色 / 完成淡出 / 错误红闪抖动(宽松忽略不阻塞) + 闪烁光标
- [x] 推进: 打完当前块自动衔接下一块, 当前token视口锚定(top 22%~62%带外平滑回中)
- [x] 键盘: 字母键优先消费, Esc退出(重渲染还原), Tab跳词; 输入框守卫保留
- [x] 竖排拒绝(toast提示), 翻页模式自动切滚动
- [x] 拾取交互: 点按钮进拾取态(悬停高亮)→点选段落居中+其余变灰→就地开始
- [x] 可见输入条: 底部悬浮, 失焦自动回焦, 状态读数(n/m)
- [x] 打字音效(v48): WebAudio合成按键blip/错误嗡声, 高级页开关默认开(持久化+备份)
- [x] 输入法真打模式(v49): 中文打字方式二态(拼音对照/输入法真打); 真打按上屏字面流式比对, 长词组一次上屏横跨多token批量点亮; 日文汉字读音问题随IME上屏消解; 输入条实时显示下一词读音提示
- [ ] HUD(WPM/正确率) — 可选后续
- [x] 测试: 中英日混合书断言 拾取/大小写键入推进/错键宽松/Tab跨段/拼音衔接ruby罗马音/章末收束/Esc退出 + 真打跨token/中文整词/日文字面比对

**v49 附带修复**:
- closeBook 复位会话级模式(typing/marker), 修残留状态吞掉书架Esc
- 键盘兜底统一挂window(移除document版避免双触发); bindEl防御式绑定助手(资产混载时序免疫)
- 整书模式退出打字强制重建文档(restyle不清DOM导致span残留/token翻倍)
- 导图高亮节点布局修复(挂载挪至布局前)+视图位置记忆+悬停全文

## 阶段三：打字机定时模式（另一种自动阅读）[ ]

定时自动逐行步进，当前行居中锚定——与现有连续爬行的自动阅读互补。

- [ ] 工具栏「打字机」按钮，与自动阅读互斥（开一个关另一个）；仅滚动模式可用，翻页模式下开启自动切滚动；竖排暂不支持（提示 toast）
- [ ] rAF 循环: 每 interval(ms, 由速度滑杆映射, 公式如 max(800, 6300-speed*600)) 平滑滚动 步长=2行×行高px
- [ ] 行高测定: iframe body computedStyle lineHeight/fontSize，回退 fontSize*state.lineHeight；步进起点 snap 到行边界倍数（防半行）
- [ ] 章末行为与 autoTick 对齐: 自动进入下一章或停止
- [ ] 运行时态不持久化（与 state.auto 一致）；speedCtl 滑杆共用
- [ ] 测试: 开启后 sleep 数秒断言 scrollY 递增且对齐行边界；关闭后静止；互斥关系

## 已完成附加功能

### 逐字阅读（刮刮乐）[x]（v56）

- 工具栏刮刮乐按钮（打字模式旁），与会话互斥；正文按块涂覆为「刮刮乐小格」（主题派生灰，深色主题自动转亮银），鼠标划过逐字点亮显现
- 涂覆为视图懒加载：`IntersectionObserver(rootMargin:±100%)` 观测书块，进入视野才包裹 `<span class="scr">`，超大章节 / 整书模式可用免全量包裹卡顿
- 逐字精确显现：`pointermove` 挂 iframe 文档，只点亮光标下字符（邻字不动）；滚动/翻页/竖排均兼容（IO 兜底新视野自动涂覆）
- 复位：`scratchReset()` 移除样式+监听+重置点亮，span 残留无样式不可见，换章重渲染自然带走；Esc / 按钮退出
- 测试: 冒烟断言 涂层透明+灰底 / 单字精确点亮邻字不动 / 拖过多字依次点亮 / Esc 还原

### 马克笔高亮 [x]（v43）

- 工具栏马克笔按钮: 开启后鼠标变马克笔光标, 划选正文即按当前色标记; 色板5色即时切换(记忆)
- 持久化: 文本锚点(前缀16字+文本+后缀24字)存localStorage, 重开书/重渲染自动回贴
- 删除: 非标记模式下点已有高亮 → toast带删除动作
- 导图联动: 高亮文本成为所属目录单元下的🖊子节点(背景=高亮色), 点击跳回原文定位

## 已搁置

- EPUB CFI 标准进度定位（用户决策：现 {i,u,r} 够用抗重排，等需要书签互通再做）
- 打字模式真实环境联调（用户暂停: 功能已实现且自动化测试全绿, 待后续排查用户侧环境）
